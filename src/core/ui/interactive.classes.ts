/** Botones icónicos sin clase `.btn` (tablas, tooltips, menús). */
export const UI_ICON_BUTTON_CLASSES = 'ui-icon-btn'

/** Botón de acción en filas de tabla: área táctil e icono de 32×32 px. */
export const UI_TABLE_ICON_BUTTON_CLASSES =
  'ui-icon-btn inline-flex size-8 shrink-0 items-center justify-center rounded-full transition-colors'

/** Contenedor con disparadores interactivos personalizados (slot de tooltip, etc.). */
export const UI_POINTER_CHILDREN_CLASSES =
  '[&_button]:cursor-pointer [&_a]:cursor-pointer [&_button:disabled]:cursor-not-allowed [&_a[aria-disabled=true]]:cursor-not-allowed'
